it "dobleDelSiguiente 3 == 8" $ do 
  dobleDelSiguiente 3 `shouldBe` 8
 
it "dobleDelSiguiente 1 == 4" $ do 
  dobleDelSiguiente 1 `shouldBe` 4
  
it "dobleDelSiguiente 10 == 22" $ do 
  dobleDelSiguiente 10 `shouldBe` 22